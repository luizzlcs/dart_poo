class Pessoa {
  final String nome;
  final int idade;

  // Para o construtor receber cons é preciso atribuir
  // final aos atributos da classe.
  const Pessoa({
    required this.nome,
    required this.idade,
  });

  
}
